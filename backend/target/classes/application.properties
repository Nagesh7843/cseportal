# Spring Boot Application Config
spring.application.name=sit-cse-portal-backend
server.port=8080

# PostgreSQL sitportaldb Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/sitportaldb
spring.datasource.username=postgres
spring.datasource.password=N@gesh7843
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate & JPA Properties
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Jackson JSON Formatter
spring.jackson.serialization.indent_output=true
